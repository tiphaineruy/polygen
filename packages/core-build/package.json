{
  "name": "@callstack/polygen-core-build",
  "version": "0.0.1",
  "description": "The Core libraries of Polygen for generating code",
  "main": "./dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "postinstall": "tsc -p tsconfig.json || true",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@callstack/polygen-codegen": "workspace:^",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "consola": "^3.2.3",
    "ora": "^8.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "eslintIgnore": [
    "node_modules/",
    "dist/"
  ]
}
